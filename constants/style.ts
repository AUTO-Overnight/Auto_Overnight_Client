import { Dimensions } from "react-native";

export const { width: SCREEN_WIDTH } = Dimensions.get("window");

export const BONUS_POINT_COLORS = {
  background: "#fff",
  header: "#f6f8ff",
  penalty: "#fff5f5",
}